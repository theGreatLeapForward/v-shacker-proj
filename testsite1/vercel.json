{
    "builds": [{
        "src": "testsite1/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "testsite1/wsgi.py"
        }
    ]
}
